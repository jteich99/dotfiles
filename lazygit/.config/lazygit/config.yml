gui:
  # showIcons: true
  nerdFontsVersion: "3"
  sidePanelWidth: 0.2 # gives you more space to show things side-by-side
  theme:
      selectedLineBgColor:
        - reverse 
      selectedRangeBgColor:
        - reverse 
git:
  paging:
    colorArg: never
    pager: ydiff -p cat -s --wrap --width={{columnWidth}} --show-syntax-themes --dark
    # pager: delta --show-syntax-themes --dark
